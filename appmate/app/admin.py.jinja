from django.contrib import admin

from app.models import *
{% for model in models %}


@admin.register({{ model.name }})
class {{ model.name }}Admin(admin.ModelAdmin):
    list_display = {{ model.name}}._fields()
{% endfor %}
